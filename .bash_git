# Currently untestable, since savehistory is broken. 
# All-in-one push-to-git command with autogenerated commit messages.
YOLO(){
       git add -A
       git commit -m '$(curl http://whatthecommit.com/index.txt)'
       git push --all -f
}
